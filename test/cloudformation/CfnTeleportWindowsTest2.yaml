AWSTemplateFormatVersion: '2010-09-09'
Description: CfnTeleportWindowsTest2 - Minimal target stack for Windows binary testing

Resources:
  # CloudFormation requires at least one resource - this is a minimal placeholder
  # Real test resources will be moved here from WindowsTest1 during integration tests
  WaitConditionHandle:
    Type: AWS::CloudFormation::WaitConditionHandle

Outputs:
  StackFormat:
    Description: Format of this stack template
    Value: YAML
